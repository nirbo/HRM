Fix HRM config indentation.

Summary of changes:
- Nested approx_grad/halting/gate parameters back under model.hrm in default, fp8, and moe configs so the settings load correctly.
- Logged the correction in the statefile.

Testing & validation:
- Config-only change.

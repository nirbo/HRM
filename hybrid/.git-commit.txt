Title: Allow CLI overrides and zero LoRA dropout for RWKV7 runs

Summary:
- add --override flag to trainer; parse key=value pairs with yaml and apply via OmegaConf.update before runtime.
- default rwkv7 LoRA dropout to 0.0 (matches memory tuning) and note change in statefile.

Testing:
- PYTHONPATH=src ./venv/bin/python -m compileall src/hrm_lm/training/train.py

Fix README Mermaid diagram label so GitHub renders architecture flowchart

Summary
- Replace the CLS node label to avoid nested closing bracket that broke GitHub Mermaid parsing.
- Diagram now renders correctly while retaining architecture context.

Files
- README.md — adjust HRM diagram label (`CLS Pool`).

Handle local tokenizer files in chunker

Summary:
- load standalone tokenizer.json via PreTrainedTokenizerFast so chunk_text_dataset.py works without a config directory
- set fallback <bos>/<eos>/<pad> tokens when missing
- log the update in llm.state

Files touched:
- scripts/chunk_text_dataset.py
- llm.state

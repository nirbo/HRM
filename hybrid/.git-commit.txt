Trainer QoL: checkpoint rotation, auto-resume, run naming, best model tracking

Summary
- Added CLI flags for run naming, checkpoint rotation, and best-model persistence; checkpoints now auto-resume when rerun.
- Implemented checkpoint FIFO limit, best-model directory maintenance, and automatic detector for completed runs.
- Documented new workflow, including auto-resume semantics and extended CLI table.

Files
- src/hrm_lm/training/train.py — CLI updates, auto-resume, checkpoint limit enforcement, best-model saver.
- TRAINING.md — refreshed instructions covering run names, rotation, and new parameters.
- llm.state — updated task log (not intended for commit).

Default HRM config fallbacks.

Summary of changes:
- HRMLanguageModel now reads HRM config via dict.get with sensible defaults, avoiding KeyErrors when older configs are missing fields.
- Updated CUDA.md FP8 section/state to mention padding and BF16 expert fallback.

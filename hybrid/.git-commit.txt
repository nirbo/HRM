Document gated HRM warmup and add reasoning dataset builder

- add scripts/build_reasoning_dataset.py to export GSM8K, MBPP, and ScienceQA prompts via the venv with datasets/pillow
- create REASON-TRAINING.md detailing Stage B/C curriculum steps, including export/tokenize/merge commands in executable order
- document the Stage A gated HRM warmup recipe in TRAINING.md for reproducibility and append recent actions to llm.state

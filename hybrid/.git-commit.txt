Commit Summary:
- add merge_prepared_batches.py to stream chunked dataset outputs into consolidated train/val/meta artifacts with optional retention
- extend prepare_language_dataset to support custom text fields, json inputs, and reuse tokenizer (prior change included)
- accelerate dataset loading via multiprocessing option and heartbeat logging; expose --dataset_workers CLI flag
- document merge workflow, dataset_workers flag, and loader progress in TRAINING.md; ignore redpajama-data checkout
- update llm.state with detailed action log per AGENTS.md

Testing / Validation:
- PYTHONPATH=src ./venv/bin/python -m compileall src/hrm_lm/training/train.py
- PYTHONPATH=src ./venv/bin/python -m compileall scripts/merge_prepared_batches.py

Title: Recalculate scheduler for batch 2 / accum 16 baseline

Summary:
- set rwkv7_lora defaults to batch_size=2, eval_batch_size=2, grad_accum_steps=16.
- rescale warmup/stable/decay steps to 31/137/79 (247 optimizer steps) for the 7,899-sample split.
- log scheduler change in llm.state.

Testing:
- n/a (config update only)

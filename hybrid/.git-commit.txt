Make --dry_run a flag

Summary
- Converted trainer `--dry_run` argument to a boolean flag (`action='store_true'`) and updated doc examples accordingly.
- Re-ran dry-run command and pytest to verify usage remains correct.

Files
- src/hrm_lm/training/train.py — CLI flag update.
- TRAINING.md — refreshed dry-run examples to match flag usage.
- llm.state — updated task log (not for commit).

Adjust FP8 config halting weight.

Summary of changes:
- Raised `train.model.hrm.halting_weight` to 0.05 in `src/hrm_lm/configs/fp8.yaml` for the current FP8 prototype run.

Testing & validation:
- Config-only change; reused existing FP8 training run.

Title: Remove RWKV backend integration

Summary:
- Delete the RWKV encoder implementation and vendored CUDA kernels, reverting the encoder stack to transformer/mamba2 options only.
- Drop the `--encoder_backend` CLI flag and tighten config validation so unsupported backends raise early.
- Trim documentation and config comments to reflect the supported backends and update the state log accordingly.
- Verified trainer dry-run succeeds after removal (`PYTHONPATH=src ./venv/bin/python -m hrm_lm.training.train --dry_run`).

Testing:
- PYTHONPATH=src ./venv/bin/python -m hrm_lm.training.train --dry_run

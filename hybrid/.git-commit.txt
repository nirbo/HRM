RWKV-7 backend integration groundwork

- add setup script that prepares venv and clones required RWKV upstream repos
- allow tracking of new backend module by adjusting gitignore rules
- introduce RWKV7 encoder wrapper and wire it into LMEncoder
- document integration steps and log progress in statefile

Title: Log eval batch push in statefile

Summary:
- Append llm.state with a record of the `git push` operation for the eval batch size update.
- No code or documentation behavior changes.

Files Touched:
- llm.state
- .git-commit.txt
- llm.state.bak (local backup, untracked)

Validation:
- Not applicable (metadata-only).

Follow-ups:
- None.

Handle conversation datasets in language preprocessor

Summary:
- extend build_sample_text to accept instruction/output pairs and conversation turn arrays when prompt/response fields are missing.
- label conversation turns with speaker names and normalize whitespace so OpenHermes-style records yield usable text.
- regenerate the OpenHermes processed split, now emitting ~1.22M train and ~12k val samples for slice evaluation.

Tests:
- python -m compileall scripts/prepare_language_dataset.py

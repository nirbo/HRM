Allow new halting head when loading Stage B checkpoints

- load checkpoints with strict=False and log missing/unexpected keys so new modules (halt head) initialize cleanly
- record the change in llm.state and validate via python -m compileall

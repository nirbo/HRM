Commit Summary:
- show per-batch progress (count + ETA) during validation sweeps for better visibility
- log change in llm.state

Testing / Validation:
- PYTHONPATH=src ./venv/bin/python -m compileall src/hrm_lm/training/train.py
